{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://delta-v-beyond-sector-3-26/schema/common",
    "title": "Common Type Definitions",
    "description": "AGENTS: before modifying this schema, read AGENTS.md at the repository root.\n\nShared type definitions used across multiple schemas. Referenced via cross-schema $ref.",
    "type": "object",
    "additionalProperties": false,
    "$defs": {
        "vec3": {
            "description": "3-component vector (x, y, z) in metres. Each component uses value+unit format per ADR-0008.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "x",
                "y",
                "z"
            ],
            "properties": {
                "x": {
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/length"
                },
                "y": {
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/length"
                },
                "z": {
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/length"
                }
            }
        },
        "quat": {
            "description": "Unit quaternion (x, y, z, w) for rotation. Defaults to identity (no rotation).",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "x",
                "y",
                "z",
                "w"
            ],
            "properties": {
                "x": {
                    "description": "X component of the quaternion imaginary part. Default 0.0.",
                    "type": "number",
                    "default": 0.0
                },
                "y": {
                    "description": "Y component of the quaternion imaginary part. Default 0.0.",
                    "type": "number",
                    "default": 0.0
                },
                "z": {
                    "description": "Z component of the quaternion imaginary part. Default 0.0.",
                    "type": "number",
                    "default": 0.0
                },
                "w": {
                    "description": "W component (real/scalar part) of the quaternion. Default 1.0 (identity).",
                    "type": "number",
                    "default": 1.0
                }
            }
        },
        "orbital_parameters": {
            "description": "Orbital movement parameters for celestial bodies. All values in SI units (metres, seconds, radians) after conversion. These are world-specific, not template-specific.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "orbital_parent",
                "orbital_distance",
                "orbital_period"
            ],
            "properties": {
                "orbital_parent": {
                    "description": "ID of the parent body this entity orbits. If absent, the entity does not orbit (e.g., sun, free-floating body).",
                    "type": "string"
                },
                "orbital_distance": {
                    "description": "Orbital distance (semi-major axis) in metres.",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/length"
                },
                "orbital_period": {
                    "description": "Orbital period in seconds.",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/time"
                },
                "orbital_eccentricity": {
                    "description": "Orbital eccentricity (0 = circular, 0.1-0.9 = increasingly elliptical). Default: 0 (circular).",
                    "type": "number",
                    "default": 0.0,
                    "minimum": 0.0,
                    "maximum": 0.99
                },
                "orbital_inclination": {
                    "description": "Orbital inclination in degrees (angle above/below the ecliptic plane). Default: 0 (in ecliptic).",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/angle",
                    "default": {
                        "value": 0.0,
                        "unit": "deg"
                    }
                },
                "initial_orbital_angle": {
                    "description": "Starting angle in the orbital plane at time zero, in degrees. Default: 0.",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/angle",
                    "default": {
                        "value": 0.0,
                        "unit": "deg"
                    }
                },
                "rotation_period": {
                    "description": "Rotation period in hours. None means no rotation.",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/time"
                },
                "axial_tilt": {
                    "description": "Axial tilt (obliquity) in degrees. Angle between rotation axis and orbital axis. Default: 0 (no tilt).",
                    "$ref": "https://delta-v-beyond-sector-3-26/schema/units#/$defs/angle",
                    "default": {
                        "value": 0.0,
                        "unit": "deg"
                    }
                }
            }
        }
    }
}