{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://delta-v-beyond-sector-3-26/schema/ship",
    "title": "Base ship template",
    "description": "AGENTS: before modifying this schema, read AGENTS.md at the repository root.\n\nDefines common ship properties shared by all ship types. Physical quantities follow ADR-0008 (value+unit objects). Allowed units are defined in the central units registry (assets/json/schema/units.schema.json).",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "entity_type",
        "mass",
        "mesh",
        "propulsion"
    ],
    "properties": {
        "entity_type": {
            "description": "Entity type discriminator. Must be 'ship'.",
            "type": "string",
            "const": "ship"
        },
        "mass": {
            "description": "Ship mass in kilograms. Required per ADR-0009 (every rigid body MUST have mass) and ADR-0014 (gameplay values in JSON).",
            "$ref": "#/$defs/physical_quantity_mass"
        },
        "inertia_scale": {
            "description": "Dimensionless multiplier on the default spherical inertia tensor. 1.0 = sphere with radius 1m. Higher values simulate mass concentrated farther from center.",
            "type": "number",
            "exclusiveMinimum": 0.0,
            "default": 1.0
        },
        "mesh": {
            "description": "Mesh geometry loaded from glTF file. Required; missing glTF is a hard error (ADR-0013).",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "path"
            ],
            "properties": {
                "path": {
                    "type": "string",
                    "description": "Path to glTF file relative to assets/ (e.g., templates/ships/space-fighter-comrade1280/mesh.glb). Required; missing file is a hard error."
                }
            }
        },
        "propulsion": {
            "description": "Propulsion system configuration. Defines main thrusters (forward/backward) and maneuvering thrusters (rotation + strafe). Required per ADR-0014 (gameplay values in JSON).",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "main_thrusters",
                "maneuvering_thruster"
            ],
            "properties": {
                "main_thrusters": {
                    "description": "Array of main thruster configurations. For M2, exactly one thruster is active. In M3+, the player can switch between thrusters (e.g. cruise vs. combat mode).",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "$ref": "#/$defs/main_thruster"
                    }
                },
                "maneuvering_thruster": {
                    "description": "Maneuver thruster (RCS) configuration for rotation and lateral/vertical strafe.",
                    "$ref": "#/$defs/maneuvering_thruster"
                }
            }
        }
    },
    "$defs": {
        "physical_quantity_mass": {
            "description": "Physical quantity with mass unit (kg). Per ADR-0008. See units.schema.json for the central unit registry.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "unit"
            ],
            "properties": {
                "value": {
                    "description": "Mass value. Must be positive and finite.",
                    "type": "number",
                    "exclusiveMinimum": 0.0
                },
                "unit": {
                    "description": "Unit of mass. Must be 'kg' (see units.schema.json).",
                    "type": "string",
                    "const": "kg"
                }
            }
        },
        "physical_quantity_force": {
            "description": "Physical quantity with force unit (Newtons). Per ADR-0008. See units.schema.json for the central unit registry.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "unit"
            ],
            "properties": {
                "value": {
                    "description": "Force value. Must be non-negative and finite.",
                    "type": "number",
                    "minimum": 0.0
                },
                "unit": {
                    "description": "Unit of force. Must be 'N' (see units.schema.json).",
                    "type": "string",
                    "const": "N"
                }
            }
        },
        "physical_quantity_torque": {
            "description": "Physical quantity with torque unit (Newton-meters). Per ADR-0008. See units.schema.json for the central unit registry.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "unit"
            ],
            "properties": {
                "value": {
                    "description": "Torque value. Must be non-negative and finite.",
                    "type": "number",
                    "minimum": 0.0
                },
                "unit": {
                    "description": "Unit of torque. Must be 'N⋅m' (see units.schema.json).",
                    "type": "string",
                    "const": "N⋅m"
                }
            }
        },
        "main_thruster": {
            "description": "A main thruster configuration. Produces forward and backward linear force along the ship's longitudinal axis (-Z forward per ADR-0006).",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "id",
                "type",
                "max_forward_thrust",
                "max_backward_thrust"
            ],
            "properties": {
                "id": {
                    "description": "Unique identifier for this thruster within the ship. Used to select the active thruster in M3+.",
                    "type": "string"
                },
                "type": {
                    "description": "Thruster type. Determines fuel consumption, heat, sound, and VFX in M3+. For M2, this is metadata only.",
                    "type": "string",
                    "enum": [
                        "chemical",
                        "ion",
                        "fusion",
                        "antimatter",
                        "scram"
                    ]
                },
                "max_forward_thrust": {
                    "description": "Maximum forward thrust in Newtons (applied along -Z local axis).",
                    "$ref": "#/$defs/physical_quantity_force"
                },
                "max_backward_thrust": {
                    "description": "Maximum backward/reverse thrust in Newtons (applied along +Z local axis). Typically lower than forward thrust.",
                    "$ref": "#/$defs/physical_quantity_force"
                }
            }
        },
        "maneuvering_thruster": {
            "description": "Maneuver thruster (RCS) configuration. Produces torque for rotation (pitch, yaw, roll) and force for lateral/vertical strafe.",
            "type": "object",
            "additionalProperties": false,
            "required": [
                "type",
                "max_torque",
                "max_strafe_thrust"
            ],
            "properties": {
                "type": {
                    "description": "Maneuvering thruster type. Determines response characteristics in M3+. For M2, this is metadata only.",
                    "type": "string",
                    "enum": [
                        "rcs",
                        "vernier",
                        "gimbaled",
                        "electromagnetic"
                    ]
                },
                "max_torque": {
                    "description": "Maximum torque in Newton-meters applied around each rotation axis (pitch=X, yaw=Y, roll=Z per ADR-0006).",
                    "$ref": "#/$defs/physical_quantity_torque"
                },
                "max_strafe_thrust": {
                    "description": "Maximum strafe thrust in Newtons applied along each lateral/vertical axis (left/right=X, up/down=Y). Same value for all strafe directions.",
                    "$ref": "#/$defs/physical_quantity_force"
                }
            }
        }
    }
}