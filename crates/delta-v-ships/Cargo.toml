# See AGENTS.md
[package]
name = "delta-v-ships"
version = "0.0.1"
edition = "2021"
rust-version = "1.88"
description = "Ship types and ship-specific systems"
license = "GPL-3.0-or-later"

[dependencies]
bevy = { workspace = true }
log = { workspace = true }
serde_json = { workspace = true }
delta-v-core = { path = "../delta-v-core", version = "0.0.1" }
delta-v-world = { path = "../delta-v-world", version = "0.0.1" }

[lints]
rust.unsafe_code = "warn"
clippy.all = "warn"
clippy.pedantic = "warn"
clippy.nursery = "warn"
